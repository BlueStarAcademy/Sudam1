# 공통 환경 변수 예시
NODE_ENV=production

# Database connection (Prisma / Supabase)
# Replace with your Supabase-provided connection string.
# Example: postgresql://postgres:password@db.<hash>.supabase.co:5432/postgres
DATABASE_URL=

# KataGo 설정 (docker-compose에서 override 가능)
KATAGO_NUM_ANALYSIS_THREADS=4
KATAGO_NUM_SEARCH_THREADS=8
KATAGO_MAX_VISITS=500
KATAGO_NN_MAX_BATCH_SIZE=8

# 이메일 서비스 설정 (AWS SES)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
EMAIL_FROM=noreply@yourdomain.com

# SMTP 설정 (개발 환경용, AWS SES 대신 사용 가능)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# 카카오 로그인 설정
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
KAKAO_REDIRECT_URI=https://yourdomain.com/auth/kakao/callback

# 프론트엔드 URL (이메일 링크용)
FRONTEND_URL=https://yourdomain.com


